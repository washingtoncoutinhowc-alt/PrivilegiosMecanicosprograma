# Arquivos para enviar ao GitHub

Envie estes arquivos e pastas:

- `app/`
- `firebase.json`
- `firebaserc-example.json`
- `.gitignore`
- `GITHUB-ARQUIVOS.md`

Nao precisa enviar:

- `work/`
- `outputs/`

Observacao: arquivos que comecam com ponto, como `.firebaserc.example`, podem aparecer como ocultos no Windows. Use `firebaserc-example.json` se for fazer upload pelo navegador.
